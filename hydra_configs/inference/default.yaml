inference_name: zesty-surf-38_epoch-840
checkpoint_path: /home/ayeluru/vascular-superenhancement-4d-flow/working_dir/all_patients/runs/2025-09-19/23-36-14/checkpoints/epoch_0840.pt
output_dir: ${path_config.base_working_dir}/${path_config.project_name}/working_dir/inference/${inference.inference_name}
patch_size: [96, 96, 48] # TODO: probably should load patch size from the loaded model rather than hardcoding it. would need to save the patch size in the checkpoint.
patch_overlap: 16
patch_aggregation_overlap_mode: "hann"
num_workers: 1
batch_size: 200
all_test_patients: null
overwrite: false

# Patient parameters for inference
patient_id: null  # Will be overridden from command line
time_point: 3     # Default time point, can be overridden